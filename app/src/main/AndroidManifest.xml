<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prueva1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activityes.MenuActivity"></activity>
        <activity
            android:name=".Activityes.animationActivity"
            android:theme="@style/FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activityes.CollapsingToolbarv2" />
        <activity android:name=".Activityes.collapsingToolbar" />
        <activity
            android:name=".Activityes.MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>